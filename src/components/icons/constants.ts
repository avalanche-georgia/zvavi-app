import type { Size } from '@components/types'

export const containerClassesBySize: Record<Size, string> = {
  lg: 'size-7',
  md: 'size-6',
  sm: 'size-5',
}

export const iconSizesBySize: Record<Size, number> = {
  lg: 24,
  md: 20,
  sm: 16,
}
